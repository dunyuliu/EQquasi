#! /bin/bash

# This script contains commands to install 
#   necessary packages for EQquasi on Ubuntu.
apt-get update
apt-get install git mlocate vim mpich
apt-get install python3 make python3-pip
apt-get install libnetcdf-dev libnetcdff-dev
apt-get install libmumps-dev 
pip install numpy netCDF4 matplotlib xarray